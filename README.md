offregister_certificate
===============
This package follows the offregister specification to generate certificates

## Install dependencies

    pip install -r requirements.txt

## Install package

    pip install .
